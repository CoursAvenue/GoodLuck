# Welcome to the finished hiring app!


### Setup DB

    rake db:create
    rake db:migrate
    rake db:seed

### Install && Running
*You need to have sass installed*
- Fork or download this repository.
- `cd` to the project's location
- run `npm install`
- run `npm start`
